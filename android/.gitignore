gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Upload keystore IS committed — it's only used to sign uploads to Play,
# which re-signs everything with its own App Signing Key. See RELEASE.md.
# key.properties
# **/*.keystore
# **/*.jks
